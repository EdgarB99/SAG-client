import { NombreLotePipe } from './nombre-lote.pipe';

describe('NombreLotePipe', () => {
  it('create an instance', () => {
    const pipe = new NombreLotePipe();
    expect(pipe).toBeTruthy();
  });
});
