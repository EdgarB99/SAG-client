import { ActualizarPesoPipe } from './actualizar-peso.pipe';

describe('ActualizarPesoPipe', () => {
  it('create an instance', () => {
    const pipe = new ActualizarPesoPipe();
    expect(pipe).toBeTruthy();
  });
});
