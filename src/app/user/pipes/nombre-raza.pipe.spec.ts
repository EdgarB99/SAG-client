import { NombreRazaPipe } from './nombre-raza.pipe';

describe('NombreRazaPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreRazaPipe();
    expect(pipe).toBeTruthy();
  });
});
